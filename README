Common Lisp code for building Quicklisp "dists". The Lisp function
COMPILE-DIST takes the name of a file containing a DEFINE-DIST form
(see monkeylib.dist in my monkeydist project for an example) and then
builds the three files required by Quicklisp in an appropriate
directory.